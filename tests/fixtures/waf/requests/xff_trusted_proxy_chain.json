{
  "id": "xff_trusted_proxy_chain",
  "description": "Chain of X-Forwarded-For IPs where first should be trusted as client IP",
  "entry_point": "header:X-Forwarded-For",
  "expected_result": "pass",
  "attack_type": "none",
  "notes": "Trusted proxy chain - first IP in XFF should be used as client IP",
  "request": {
    "method": "GET",
    "path": "/api/data",
    "headers": [
      ["X-Forwarded-For", "10.0.0.1, 10.0.0.2, 10.0.0.3"],
      ["X-Real-IP", "10.0.0.1"],
      ["Host", "example.com"]
    ],
    "query_string": null,
    "body": null
  }
}