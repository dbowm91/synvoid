{
  "id": "multipart_field_attack",
  "description": "SQL injection payload in multipart form field - current detector gap",
  "entry_point": "post_body",
  "expected_result": "pass",
  "attack_type": "none",
  "notes": "SQL injection in multipart form field - known detector gap to fix",
  "request": {
    "method": "POST",
    "path": "/upload",
    "headers": [
      ["Host", "example.com"],
      ["Content-Type", "multipart/form-data; boundary=----WebKitFormBoundary"]
    ],
    "query_string": null,
    "body_file": "../bodies/multipart_sqli.txt"
  }
}