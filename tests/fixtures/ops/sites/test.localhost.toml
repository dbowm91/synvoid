[site]
domains = ["test.localhost"]

[site.upstream]
default = "http://127.0.0.1:19999"

[[site.listen]]
port = 18080

[ratelimit]
mode = "isolated"

[ratelimit.ip]
per_second = 50
per_minute = 500

[blocked]
paths = ["/.env", "/.git"]
