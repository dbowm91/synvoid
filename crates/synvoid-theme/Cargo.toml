[package]
name = "synvoid-theme"
version = "0.1.0"
edition = "2021"
license.workspace = true
repository.workspace = true
description = "Theme rendering and template engine"

[dependencies]
synvoid-config = { path = "../synvoid-config" , version = "0.1" }
rand = "0.9"
chrono = { version = "0.4", features = ["serde"] }
