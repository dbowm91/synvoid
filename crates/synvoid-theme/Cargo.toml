[package]
name = "synvoid-theme"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
synvoid-config = { path = "../synvoid-config" }
rand = "0.9"
chrono = { version = "0.4", features = ["serde"] }
