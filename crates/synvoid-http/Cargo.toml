[package]
name = "synvoid-http"
version = "0.1.0"
edition = "2021"
license.workspace = true
repository.workspace = true
description = "HTTP/1 and HTTP/3 server pipeline"

[features]
mesh = ["synvoid-config/mesh", "dep:synvoid-mesh", "synvoid-mesh/mesh"]
dns = []

[dependencies]
synvoid-core = { path = "../synvoid-core" , version = "0.1" }
synvoid-config = { path = "../synvoid-config" , version = "0.1" }
synvoid-metrics = { path = "../synvoid-metrics" , version = "0.1" }
synvoid-waf = { path = "../synvoid-waf" , version = "0.1" }
synvoid-challenge = { path = "../synvoid-challenge" , version = "0.1" }
synvoid-http-client = { path = "../synvoid-http-client" , version = "0.1" }
synvoid-app-server = { path = "../synvoid-app-server" , version = "0.1" }
synvoid-app-handlers = { path = "../synvoid-app-handlers" , version = "0.1" }
synvoid-proxy = { path = "../synvoid-proxy" , version = "0.1" }
synvoid-upload = { path = "../synvoid-upload" , version = "0.1" }
synvoid-plugin-runtime = { path = "../synvoid-plugin-runtime" , version = "0.1" }
synvoid-utils = { path = "../synvoid-utils" , version = "0.1" }
synvoid-mesh = { path = "../synvoid-mesh", optional = true , version = "0.1" }
synvoid-serverless = { path = "../synvoid-serverless" , version = "0.1" }
synvoid-static-files = { path = "../synvoid-static-files" , version = "0.1" }
synvoid-ipc = { path = "../synvoid-ipc" , version = "0.1" }
ahash = "0.8"
anyhow = "1"
bytes = "1"
async-trait = "0.1"
futures = "0.3"
dashmap = "7.0.0-rc2"
http = "1"
http-body = "1"
http-body-util = "0.1"
axum = { workspace = true, features = ["json", "macros"] }
hyper = { version = "1", features = ["http1", "http2", "server"] }
hyper-util = { version = "0.1", features = ["tokio", "server-auto", "server-graceful", "http1", "http2"] }
tokio = { version = "1", features = ["rt", "net", "time", "sync", "macros", "io-util"] }
tokio-tungstenite = "0.28"
tower = { version = "0.5", features = ["util"] }
tracing = "0.1"
metrics = "0.22"
httparse = "1"
chrono = "0.4"
rand = "0.9"
sha1 = "0.10"
base64 = "0.22"
h3 = "0.0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"

[dev-dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time"] }
