// Windows operator helpers: firewall rules, interface resolution, Wintun loader.
// Canonical owner (Phase 32); the root `src/platform/windows/` copies were removed.

pub mod firewall;
pub mod interface_resolver;
pub mod wintun;
