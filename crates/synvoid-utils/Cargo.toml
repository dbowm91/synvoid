[package]
name = "synvoid-utils"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = "1"
serde = { version = "1", features = ["derive"] }
postcard = { version = "1", features = ["alloc"] }
parking_lot = "0.12"
