[package]
name = "synvoid-tarpit"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
parking_lot = "0.12"
rand = "0.9"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["sync"] }

[dev-dependencies]
serde_json = "1"
